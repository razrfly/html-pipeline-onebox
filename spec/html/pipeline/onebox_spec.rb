require "spec_helper"

RSpec.describe Html::Pipeline::Onebox do
  it "has a version number" do
    expect(Html::Pipeline::Onebox::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
